import 'package:flutter/material.dart';

/// A utility class that provides a collection of constant color values
/// to be used throughout the application for consistent color theming.
class ColorPalleteHelper {
  /// Lightest shade of the primary color, suitable for backgrounds.
  static const Color primary50 = Color(0xFFEFF8FF);

  /// Light shade of the primary color, useful for highlighting components.
  static const Color primary100 = Color(0xFFD1E9FF);

  /// Primary color, ideal for main interactive elements like buttons or links.
  static const Color primary500 = Color(0xFF2E90FA);

  /// Black color, typically used for text or dark accents.
  static const Color black = Color(0xFF000000);

  /// White color, useful for backgrounds or contrasting with dark elements.
  static const Color white = Color(0xFFFFFFFF);

  /// Standard gray color, suitable for secondary text or icons.
  static const Color gray = Color(0xFF475467);

  /// Lighter shade of gray, often used for borders or disabled elements.
  static const Color gray300 = Color(0XFFD0D5DD);

  /// Medium shade of gray, suitable for text or UI elements that need less emphasis.
  static const Color gray500 = Color(0XFF667085);

  /// Success color, commonly used for positive feedback (e.g., success messages).
  static const Color success = Color(0xFF32D583);

  /// Warning color, ideal for alerts or notifications that require attention.
  static const Color warning = Color(0xFFFDB022);

  /// Error color, used to indicate errors or critical issues in the UI.
  static const Color error = Color(0xFFc6231d);

  static const pink = Color(0xFFF670C7);
  static const pink400 = Color(0xFFF670C7);
}
